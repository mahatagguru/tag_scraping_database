# FastMCP Integration Requirements
# Install FastMCP from GitHub: pip install git+https://github.com/jlowin/fastmcp.git
# Or use the official MCP SDK: pip install mcp

# Core dependencies (if not already in main requirements.txt)
typing-extensions>=4.0.0
pathlib2>=2.3.0; python_version < "3.4"

# Official MCP SDK (alternative to FastMCP)
mcp>=1.0.0

# FastMCP dependencies
pydantic>=2.0.0
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
